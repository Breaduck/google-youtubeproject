name = "byteplus-video-proxy"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# 환경 변수 (Secret으로 설정 필요)
# wrangler secret put IMGUR_CLIENT_ID

[env.production]
name = "byteplus-video-proxy"
routes = [
  { pattern = "video-proxy.yourdomain.com", zone_name = "yourdomain.com" }
]
